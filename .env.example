# === Robot Core .env (defaults) ===
ROBOT_HOSTNAME=RPi-Robot
ROBOT_NETWORK=robot-net
VIDEO_DASHBOARD_PORT=8081
PIPER_PORT=10200
WHISPER_PORT=10300
MOSQUITTO_PORT=1883

# === Dashboard auth (generate htpasswd with setup script) ===
# DASHBOARD_USER=drew
# Run: echo "drew:$(openssl passwd -apr1)" > docker/nginx.htpasswd
