# Generated by lightweight static scan on 2026-03-03
# Top-level (non-stdlib) Python dependencies discovered in services/*.py
paho-mqtt
wyoming

# Notes:
# - stdlib modules (asyncio, wave, audioop, subprocess, threading, etc.) omitted.
# - Review native/system deps (arecord/aplay, ALSA, ROS packages) in Dockerfiles and compose.
# - Recommend pinning versions and running a pip install -r requirements.txt in each service build.
